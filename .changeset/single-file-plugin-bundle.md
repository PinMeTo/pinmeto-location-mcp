---
'@pinmeto/pinmeto-location-mcp': minor
---

Add a single-file esbuild bundle build target for plugin distribution, and resolve the package name and version at build time instead of reading package.json at runtime.

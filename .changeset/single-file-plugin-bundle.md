---
'@pinmeto/pinmeto-location-mcp': minor
---

Resolve the package name and version at build time (`src/generated/version.ts`, generated by `scripts/generate-version.mjs`) instead of reading `package.json` at runtime.
